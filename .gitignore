# Skills linked in from other repos — tracked in their own source, not here.
# (These paths exist as symlinks in ~/.agents/skills and resolve at runtime.)
/omarchy
/diagnose-crash
/cost-optimizer
/iam-least-privilege
/lambda-patterns
/sam-deploy
/serverless-scanner
/apexlang

# Local git metadata when this tree is also the working hub
/.claude
